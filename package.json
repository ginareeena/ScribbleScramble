{
    "scripts": {
        "start": "node index.js",
        "dev-server": "nodemon index.js",
        "dev-app": "cd FrontEnd && yarn start",
        "build": "cd FrontEnd && yarn build",
        "heroku-postbuild": "cd FrontEnd && npm install && npm install --only=dev --noshrinkwrap && npm run build"
    },
    "dependencies": {
        "express": "^4.17.1",
        "morgan": "^1.10.0",
        "nodemon": "^2.0.7",
        "path": "^0.12.7",
        "socket.io": "^4.0.0"
    }
}
